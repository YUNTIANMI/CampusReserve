# CampusReserve
## 开发阶段计划

版本：v1.0

## 总原则

核心目标：
> 展示完整的微信小程序开发流程。

重点：
小程序 UI → 页面/导航 → 组件化 → 生命周期 → 请求 → 登录 → 资源 → 预约 → 状态处理 → 真机测试 → 工程整理

后端只实现核心 API。

---

# Phase 0：项目准备

状态：已完成（2026-09-15）

- [x] 建立 Git 仓库
- [x] 创建 README
- [x] 创建 docs
- [x] 创建 `docs/AGENTS.md`
- [x] 创建 `docs/PROJECT_MEMORY.md`
- [x] 初始化微信小程序
- [x] 初始化 Spring Boot
- [x] 确认 MySQL
- [x] 确认真机测试方式（方式已确定：开发者工具「预览」扫码 + `baseUrl` 指向局域网 IP；实际真机验证在 Phase 9 / Phase 11）

验收：
- [x] 小程序可启动
- [x] 后端可启动
- [x] Git 可提交

产出：
- 仓库结构：`CampusReserve/`（小程序）+ `backend/`（后端）+ `docs/`（全部文档）
- 小程序：TypeScript 工程骨架，AppID `wxb97024eb0305368d`
- 后端：Spring Boot 3.5.16 + Java 17，`GET /api/health` 验证通过
- MySQL：确认 8.4.10 可用，选定 3308 实例并实测连接通过（数据层在 Phase 10 接入）
- 文档体系：五个文档统一归入 `docs/`，编号固定为 01 需求 / 02 技术设计 / 03 数据库设计（待建）/ 04 开发计划 / 05 API 契约（待建）
- 远端：GitHub `main` 分支已同步 Phase 0 全部提交（提交信息为中文 Conventional Commits）

已在本阶段修正：`docs/` 编号冲突（原 03 / 04 / 05）按上表重新约定，`docs/PROJECT_MEMORY.md` 中原记录已随修正移除。

---

# Phase 1：微信小程序基础框架

- [x] 配置页面路由
- [x] 创建首页
- [x] 创建资源列表
- [x] 创建资源详情
- [x] 创建我的预约
- [x] 创建预约详情
- [x] 建立基础组件
- [x] 建立基础样式
- [x] 建立统一请求服务
- [x] 建立 TypeScript 类型

验收：
所有核心页面可正常跳转。（已实测：`tools/e2e/e2e-phase1.js` 36/36 通过）

---

# Phase 2：首页

- [x] 顶部区域
- [x] 分类入口
- [x] 热门资源
- [x] 推荐资源
- [x] `ResourceCard`
- [x] Loading
- [x] Empty
- [x] Error
- [x] 下拉刷新

验收：
首页正常展示，点击资源可进入列表/详情。

实现说明：
- 数据经 `services/resource.ts`（`getResources`）获取，调用链 Page → Service → API。
- 后端业务 API 属于 Phase 10，本阶段由 `services/config.ts` 的 `USE_MOCK_DATA` 开关切到
  `services/mock-resource.ts` 的本地数据源；联调时把开关改为 `false` 即可切到真实接口，页面代码无需改动。
- 热门 / 推荐为前端临时划分（前 4 条热门、其余推荐）。`docs/05_api_contract.md` 确定后，
  若后端提供独立的热门 / 推荐语义，只需调整 `pages/index/index.ts` 的 `renderResources`。
- 静态检查：`tsc --noEmit` 0 错误。
- 端到端测试：真实开发者工具中 47/47 通过（`tools/e2e/e2e-phase2.js`）；
  Phase 1 回归 36/36 通过（`tools/e2e/e2e-phase1.js`）。

---

# Phase 3：资源列表

- [x] 获取资源列表
- [x] ResourceCard
- [x] 分类筛选
- [x] Loading
- [x] Empty
- [x] Error
- [x] 页面跳转
- [x] 下拉刷新

验收：
可查看资源并进入详情。

验收结论（2026-09-15 实测）：
- 端到端测试 65/65 通过（`tools/e2e/e2e-phase3.js`），Phase 1 / Phase 2 回归 36/36、47/47
- 分类筛选下推到数据源（`getResources({ type })`），不在本地过滤，结果类型与筛选一致
- 筛选栏为静态内容，loading / empty / error 三态下均保持可用（不整页替换）
- 非法 `category` 参数归一化为「全部」而非错误态
- 空态文案按筛选条件分流：全量为「暂无资源」+「重新加载」，某分类为空为「暂无该分类」且不给出注定无效的按钮
- 下拉刷新验证方式为「数据源变化能反映到页面」，证明真的重新发起了请求

---

# Phase 4：资源详情与时间选择

- [x] 图片展示
- [x] 资源信息
- [x] 日期选择
- [x] `TimeSlot`
- [x] 可用/不可用状态
- [x] 获取指定日期可用时间
- [x] 选择时间
- [x] 预约按钮状态

验收：
可查看指定资源在指定日期的时间段。

验收结论（2026-09-15 实测）：
- 端到端测试 81/81 通过（`tools/e2e/e2e-phase4.js`），Phase 1 / Phase 2 / Phase 3 回归 36/36、47/47、65/65
- 资源信息区与时间段区**各自独立四态**：切换日期失败只影响时间段区，资源名称/地点/描述不会一并消失
- 日期条（未来 7 天）为静态内容，不随任何四态变化；某个日期取不到时段时仍可切换其他日期
- 时间段状态 `AVAILABLE` / `BOOKED` / `DISABLED` 三态均可见；今天的时段按当前时间判定为「不可预约」
- 只有 `AVAILABLE` 可被选择；已选时段再次点击即取消选择；切换日期会清空已选时段
- 预约按钮状态：未选时段时禁用且点击无任何反馈，选中后文案为「预约 HH:mm-HH:mm」并可点击
- 资源不存在（接口正常返回但查无此资源）落到 empty 态而非 error 态，只给「返回上一页」
- 非法 `id`（非数字、`0`、非正整数）一律判定为参数错误并展示错误态
- 图片展示：`imageUrl` 存在时渲染 `<image>`，缺省时渲染资源类型占位块（开发期数据源不提供图片，
  见 `docs/PROJECT_MEMORY.md` §11 开发期数据源说明）

阶段边界（刻意留在后续阶段）：
- 预约按钮的**提交动作**属于 Phase 6（校验 + `POST /api/bookings`），本阶段点击只给出提示

---

# Phase 5：用户登录

- [x] 登录入口
- [x] 微信登录流程
- [x] 登录状态保存
- [x] 用户信息展示
- [x] 登录失败处理
- [x] 未登录状态处理

验收：
未登录用户进入预约流程时能正确引导登录。

验收结论（2026-09-15 实测）：
- 端到端测试 64/64 通过（`tools/e2e/e2e-phase5.js`），Phase 1 / Phase 2 / Phase 3 / Phase 4 回归
  36/36、47/47、65/65、81/81
- 登录入口两处：首页顶部用户区（点击进登录页）、我的预约页未登录引导的「去登录」按钮
- 一键登录流程：`wx.login()` 取一次性 code → `POST /api/auth/login` 换登录态（Phase 10 接真实后端，
  本阶段由 `services/mock-auth.ts` 顶替），点击后按钮进入「登录中…」，成功后自动返回来源页
- 登录态真源是 `store/auth.ts` 的模块级内存态，同时写入本地缓存（`CR_AUTH_TOKEN` / `CR_USER_INFO`）
  并镜像到 `app.globalData`；页面只读副本、在 `onShow` 同步
- 冷启动恢复：凭证与用户信息**同时有效**才算已登录；两者不匹配（如只有凭证）时回到未登录并清掉残留
- 用户信息展示：登录页展示昵称与用户 ID，头像用昵称首字占位（开发期数据源不提供头像）
- 登录失败：停在登录页并展示错误提示，按钮文案恢复为「微信一键登录」可直接重试
- 未登录处理：我的预约页展示登录引导而非空白，详情页点预约先弹「需要登录」引导、确认后进登录页，
  登录返回后**详情页已选时段仍在**（`navigateBack` 复用原页面实例）
- 退出登录：登录页二次确认后清空内存态与缓存，各页面在 `onShow` 回到未登录展示

阶段边界（刻意留在后续阶段）：
- `POST /api/auth/login` 的真实后端实现属于 Phase 10，本阶段由开发期数据源顶替
- 退出登录只清本地登录态，通知服务端失效属于 Phase 10

---

# Phase 6：创建预约

核心业务阶段。

- [x] Booking 数据模型
- [x] `POST /api/bookings`
- [x] 前端预约提交
- [x] 参数校验
- [x] 时间合法性校验
- [x] 冲突检查
- [x] 成功提示
- [x] 失败提示

异常必须正确处理：
- [x] 未登录
- [x] 时间冲突
- [x] 资源不存在
- [x] 参数错误
- [x] 非法时间

验收：
用户可以成功预约可用时间段。

验收结论（2026-09-15 实测）：
- 端到端测试 61/61 通过（`tools/e2e/e2e-phase6.js`），
  Phase 1 / Phase 2 / Phase 3 / Phase 4 / Phase 5 回归 36/36、47/47、65/65、81/81、64/64
- Booking 数据模型沿用 `types/booking.ts` 的 `Booking`（含 `id` / `resourceId` / `resourceName` /
  `location` / `date` / `startTime` / `endTime` / `status` / `createdAt`）与入参 `CreateBookingPayload`
- 前端提交：详情页 `onSubmit` 接入 `services/booking.ts` 的 `createBooking()` → `POST /api/bookings`
  （Phase 10 接真实后端，本阶段由 `services/mock-booking.ts` 顶替）；
  提交中按钮变为「提交中…」且不可再点，成功后 `wx.showToast` 提示并跳转「我的预约」
- 提交前检查的五项分工：**已登录**与**日期/时间合法**由客户端先判（不等一个来回就能给出原因），
  **资源存在**与**时间段仍可用**由服务端判（页面上的时段是加载时的快照，可能已过期或被别人约走），
  两端共用 `utils/booking.ts` 的同一份规则
- 冲突检查三层：请求的时段不在开放范围内 → 参数错误；时段状态非 `AVAILABLE` → 冲突；
  同一资源同一日期同一开始时间已有预约 → 冲突。开发期预约表落在缓存键 `CR_MOCK_BOOKINGS`，
  可用时间段会叠加它，因此「刚被约走的时段」立刻显示为已约满，不会自相矛盾
- 成功提示：toast「预约成功」+ 800ms 后跳转「我的预约」；跳转前清空已选时段，
  返回详情页时（`onShow`）重新拉取时段，避免用户对同一格再点一次必然失败的按钮
- 失败提示按错误码分流，页面内提示条 + toast 同时给出原因
  （toast 会消失，而「该时段已被预约」是需要用户据此改变行为的提示）：
  - `409001` 冲突 → 「该时间段已被预约，请选择其他时段」并**立即重取时段**
  - `404001` 资源不存在 → 「该资源不存在或已下架」并重新加载详情，落到 empty 态
  - `400001` 参数错误 / `400002` 非法时间 → 给出具体原因，保留已选时段可直接重试
  - `401` 登录态失效 → **清掉本地登录态**并引导重新登录（继续保留「已登录」只会让用户反复碰壁）
  - 网络异常 → 「网络连接失败」且保留已选时段，可原样重试（与上述业务失败区分开）
- 客户端对「已过时时段」的兜底：用户在详情页停留久了，加载时的时段快照可能已过期，
  这条只能在提交那一刻重新判定（E2E 用「把预约数据源打成网络失败、提交后仍看到『已过时』」
  证明该请求根本没发出去）

阶段边界（刻意留在后续阶段）：
- `POST /api/bookings` 的真实后端实现属于 Phase 10，本阶段由开发期数据源顶替
- 成功后跳转的「我的预约」页当前仍是 Phase 1 骨架 + Phase 5 登录引导，列表内容属于 Phase 7
- 取消预约（`DELETE /api/bookings/{id}`）属于 Phase 8

---

# Phase 7：我的预约

- [x] 获取我的预约
- [x] BookingCard
- [x] 待使用
- [x] 已完成
- [x] 已取消
- [x] 预约详情

验收：
用户只能看到自己的预约。

验收结论（2026-09-16 实测）：
- 端到端测试 63/63 通过（`tools/e2e/e2e-phase7.js`），Phase 1 ~ Phase 6 回归
  36/36、47/47、65/65、81/81、64/64、61/61，七套合计 **417** 项断言全绿
- 列表：`GET /api/bookings/my`（Phase 10 接真实后端，本阶段由 `services/mock-booking.ts`
  的 `mockGetMyBookings()` 顶替），**入参不含 userId**——用户身份由服务端从请求凭证解析，
  「只能看到自己的预约」因此是服务端边界，前端无从伪造，也更不需要在前端过滤
- 一次请求全量、页签切换只做本地过滤：三个页签是同一份数据的不同视图，
  每切一次都发请求只会让用户等，且「已完成」是按当前时刻派生的，两次请求之间会漂移
- **状态派生**：`utils/booking.ts` 的 `resolveBookingStatus()` 把「预约时段是否已结束」
  折算成展示状态——服务端只记录显式变更（创建 / 取消），「时段过去了」没人去点一下，
  直接读 `status` 会让昨天约的场地今天仍挂在「待使用」里。
  刻意**不改数据**：让 `GET` 产生写副作用不干净，真实后端哪天自行推进了也不会冲突
- 排序方向刻意不同：待使用按时间**升序**（最近要用的在最上面），
  已完成 / 已取消按**降序**（历史里越近的越可能还想点开看）
- 未登录时**不发起请求**：没有凭证时服务端无从判断身份，请求必然 401，
  既然页面已经知道未登录，就不该先转一圈 loading 再落到引导上
- 登录态失效时清掉本地登录态并引导重新登录（与详情页提交时同一条原则）
- `BookingCard` 组件与 `ResourceCard` 同构：只展示与抛 `cardtap`，不硬编码路由
- **预约详情复用 `/bookings/my` 再按 id 查找**，不新增 `GET /api/bookings/{id}`：
  技术设计 §3 的接口清单里没有它，且 `/my` 本来就只返回本人的数据，
  查不到即等于「不存在或无权访问」，无需另写一遍归属校验
  （代价是每次进详情拉一次全量列表，已记入 `PROJECT_MEMORY.md` 已知问题）

阶段边界（刻意留在后续阶段）：
- `GET /api/bookings/my` 的真实后端实现属于 Phase 10，本阶段由开发期数据源顶替
- 取消预约（`DELETE /api/bookings/{id}`）属于 Phase 8，详情页暂未放置取消按钮

---

# Phase 8：取消预约

- [ ] `DELETE /api/bookings/{id}`
- [ ] 取消确认
- [ ] 状态更新
- [ ] 时间段恢复可用
- [ ] 返回页面刷新

验收：
用户只能取消自己的有效预约。

---

# Phase 9：小程序体验优化

- [ ] 下拉刷新
- [ ] 加载反馈
- [ ] Empty State
- [ ] Error State
- [ ] Toast
- [ ] Modal
- [ ] 页面返回刷新
- [ ] 必要缓存
- [ ] 屏幕适配
- [ ] 真机测试

验收：
核心流程无明显交互问题。

---

# Phase 10：后端与数据层整理

- [ ] Controller
- [ ] Service
- [ ] Repository
- [ ] MySQL
- [ ] 统一 API 响应
- [ ] 基础异常处理
- [ ] 预约数据一致性

验收：
小程序核心业务可依赖真实后端运行。

---

# Phase 11：测试

## 小程序
- [ ] 页面跳转
- [ ] 资源加载
- [ ] 空数据
- [ ] 网络失败
- [ ] 登录
- [ ] 预约
- [ ] 取消预约
- [ ] 返回刷新
- [ ] 真机测试

## 后端
- [ ] 查询资源
- [ ] 查询可用时间
- [ ] 正常预约
- [ ] 时间冲突
- [ ] 非法时间
- [ ] 用户权限
- [ ] 取消预约

---

# Phase 12：作品集整理

- [ ] README
- [ ] 项目截图
- [ ] 页面流程图
- [ ] 系统架构图
- [ ] API 简要说明
- [ ] 数据库 ER 图
- [ ] 真机演示视频
- [ ] Git Commit 整理
- [ ] PROJECT_MEMORY 最终更新

最终 Demo：
登录 → 首页 → 资源列表 → 详情 → 选日期 → 选时间 → 预约 → 我的预约 → 预约详情 → 取消

---

# AI 协同开发流程

每个 Phase：

```text
Phase
 ↓
拆 Task
 ↓
Codex 读取项目状态
 ↓
分析任务
 ↓
提出计划
 ↓
开发
 ↓
测试
 ↓
开发者验收
 ↓
更新 PROJECT_MEMORY.md
 ↓
Git Commit
```

禁止跨阶段一次性实现大量功能。

# Phase 完成定义

- [ ] 需求实现
- [ ] 功能可运行
- [ ] 基础测试通过
- [ ] 无阻塞问题
- [ ] 相关文档更新
- [ ] `PROJECT_MEMORY.md` 更新
- [ ] Git Commit 完成
