/* ---------- 顶部区域 ---------- */

.hero {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 0 40rpx;
}

.hero__main {
  flex: 1;
  min-width: 0;
}

.hero__title {
  font-size: var(--cr-font-xxl);
  font-weight: 600;
  color: var(--cr-color-text);
}

.hero__desc {
  margin-top: 12rpx;
  font-size: var(--cr-font-md);
  color: var(--cr-color-text-secondary);
}

.hero__action {
  display: flex;
  align-items: center;
  flex: none;
  height: 64rpx;
  padding: 0 var(--cr-space-md);
  background-color: var(--cr-color-surface);
  border-radius: 999rpx;
}

.hero__action--hover {
  background-color: var(--cr-color-hover);
}

.hero__action-text {
  font-size: var(--cr-font-sm);
  color: var(--cr-color-text-secondary);
}

.hero__action-arrow {
  margin-left: 4rpx;
  font-size: var(--cr-font-sm);
  color: var(--cr-color-text-muted);
}

/* ---------- 分类入口 ---------- */

.category-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.category-item {
  box-sizing: border-box;
  width: 49%;
  padding: var(--cr-space-md);
  margin-bottom: var(--cr-space-sm);
  background-color: var(--cr-color-surface);
  border-radius: var(--cr-radius-md);
}

.category-item--hover {
  background-color: var(--cr-color-hover);
}

.category-item__label {
  display: block;
  font-size: var(--cr-font-md);
  font-weight: 600;
  color: var(--cr-color-text);
}

.category-item__desc {
  display: block;
  margin-top: 8rpx;
  font-size: var(--cr-font-sm);
  color: var(--cr-color-text-muted);
}

/* ---------- 区块标题行 ---------- */

.cr-section__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--cr-space-sm);
}

/* 覆盖 app.wxss 中 .cr-section__title 的单独下边距，避免与标题行重复留白 */
.cr-section__head .cr-section__title {
  margin-bottom: 0;
}

.cr-section__more {
  font-size: var(--cr-font-sm);
  color: var(--cr-color-text-muted);
}
